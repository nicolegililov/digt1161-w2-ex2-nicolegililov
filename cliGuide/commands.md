# Essential Git CLI Commands

## git status
Shows the current state of the repository, including changed or untracked files.

## git add
Stages file changes so they are ready to be committed.

## git commit
Saves a snapshot of the staged changes with a message describing the update.